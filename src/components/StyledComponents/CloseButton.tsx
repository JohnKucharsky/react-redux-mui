import { css, IconButton } from '@mui/material'
import { styled } from '@mui/material/styles'

const CloseButton = styled(IconButton)(css`
  position: absolute;
  top: 0.5rem;
  right: 0.5rem;
`)

export default CloseButton
