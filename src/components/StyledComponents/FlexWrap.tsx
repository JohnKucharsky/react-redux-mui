import { Box, css } from '@mui/material'
import { styled } from '@mui/material/styles'

const FlexWrap = styled(Box)(css`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  align-items: center;
`)

export default FlexWrap
